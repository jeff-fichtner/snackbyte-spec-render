# Widget Research

Generic readable document — not a spec/plan/tasks, so it renders with the
default view (TOC + prose).

## Options considered

We looked at a `<script>` tag & "smart quotes" to verify HTML escaping.

## Decision

```js
const x = 1;
```

## Diagram

```mermaid
graph TD;
  A-->B;
```

## Deeper section

### A level-3 heading

Content under a level-3 heading, which should appear in the TOC.
